<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Birthday -->
    <style name="Theme.HappyBirthday_Compose" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/purple_700</item>
    </style>

    <!-- Dice Roller -->
    <style name="Theme.DiceRoller" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/purple_700</item>
    </style>

    <!-- Woof -->
    <style name="Theme.Woof" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/grey_50</item>
    </style>

    <!-- Superheroes -->
    <style name="Theme.Superheroes" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/superheroes_background_light</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Dessert Clicker -->
    <style name="Theme.DessertClicker" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/green_800</item>
    </style>

    <!-- Unscramble -->
    <style name="Theme.Unscramble" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/white</item>
    </style>

    <!-- Cupcake -->
    <style name="Theme.Cupcake" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/pink_600</item>
    </style>

    <!-- Reply -->
    <style name="Theme.Reply" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Mars -->
    <style name="Theme.MarsPhotosTheme" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/purple_700</item>
    </style>

</resources>